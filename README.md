Another Trusttunnel client but in React Native and Native Modules.

> because I have no idea with Flutter and Flutter plugin. ✌🏼

Check out [Trusttunnel](https://github.com/TrustTunnel/TrustTunnel) for more details about server setup.

Example schema to use this application:

```
twohops://import-profile?name=<name>&login=<user>&password=<password>&ip=<ip>&domain=<domain>>&dns=https://dns.nextdns.io&remoteRules=https://example.com/rules.txt
```
